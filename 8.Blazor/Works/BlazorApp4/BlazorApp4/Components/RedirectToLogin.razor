@inject NavigationManager Navigation

@code
{
    protected override void OnInitialized()
    {
        var currentPath = Navigation.ToBaseRelativePath(
            Navigation.Uri);

        if (!string.Equals(
                currentPath,
                "Login",
                StringComparison.OrdinalIgnoreCase))
        {
            Navigation.NavigateTo("/Login");
        }
    }
}